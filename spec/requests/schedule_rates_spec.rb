require 'spec_helper'

describe "ScheduleRates" do
  describe "GET /schedule_rates" do
    it "works! (now write some real specs)" do
      pending
      get schedule_rates_path
      response.status.should be(200)
    end
  end
end
